#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi
#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi

export PATH="/workspace/flutter/bin:$PATH#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi

export PATH="/workspace/flutter/bin:$PATH"
export PUB_HOSTED_URL=https://#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi

export PATH="/workspace/flutter/bin:$PATH"
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi

export PATH="/workspace/flutter/bin:$PATH"
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi

export PATH="/workspace/flutter/bin:$PATH"
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn

# 检查 .env
if [#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi

export PATH="/workspace/flutter/bin:$PATH"
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn

# 检查 .env
if [ ! -f "/workspace/.env" ]; then
    echo "📝 创建 .env#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi

export PATH="/workspace/flutter/bin:$PATH"
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn

# 检查 .env
if [ ! -f "/workspace/.env" ]; then
    echo "📝 创建 .env 配置..."
    cat > /workspace#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi

export PATH="/workspace/flutter/bin:$PATH"
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn

# 检查 .env
if [ ! -f "/workspace/.env" ]; then
    echo "📝 创建 .env 配置..."
    cat > /workspace/.env << 'EOF'
SUPABASE_URL=https://vjwolnmswmhpxd#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi

export PATH="/workspace/flutter/bin:$PATH"
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn

# 检查 .env
if [ ! -f "/workspace/.env" ]; then
    echo "📝 创建 .env 配置..."
    cat > /workspace/.env << 'EOF'
SUPABASE_URL=https://vjwolnmswmhpxdsskrmg.supabase.co
SUPABASE#!/bin/bash
set -e

echo "🚀 启动 AI Life 移动端服务..."

# 检查 Flutter
if [ ! -d "/workspace/flutter" ]; then
    echo "📦 Flutter SDK 不存在，正在下载..."
    curl -L -o /workspace/flutter.tar.xz https://storage.flutter-io.cn/flutter_infra_release/releases/stable/linux/flutter_linux_3.44.7-stable.tar.xz
    tar xf /workspace/flutter.tar.xz -C /workspace
    git config --global --add safe.directory /workspace/flutter
    rm /workspace/flutter.tar.xz
    echo "✅ Flutter 下载完成"
fi

export PATH="/workspace/flutter/bin:$PATH"
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn

# 检查 .env
if [ ! -f "/workspace/.env" ]; then
    echo "📝 创建 .env 配置..."
    cat > /workspace/.env << 'EOF'
SUPABASE_URL=https://vjwolnmswmhpxdsskrmg.supabase.co
SUPABASE_ANON_KEY=sb_publishable_lUlFXeJ_SsHIWoWXFf1ZcA_1XwpX5X3
SUPABASE_SERVICE_ROLE_KEY=sb_secret_SYIRfCU5QvzlF8-U